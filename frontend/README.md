# CinePlex React + Tailwind Frontend

## Run
1. `cd /d C:\Users\dhyan\Downloads\cineplex\cineplex\frontend`
2. `npm install`
3. `npm run dev`
4. Open `http://localhost:5173`

## Notes
- This is a new premium web UI layer.
- Existing Swing backend/DAO/MySQL code is untouched.
- API client is ready in `src/services/api.js`.
- Set `VITE_API_BASE_URL` for your Java API adapter when available.
